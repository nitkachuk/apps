export interface ITasks {
    key: number
    text: string
    priority: number
}

export interface ITheme {
    logo: string[]
    button: string[]
    colors: string[][]
    toggleModeButton: string[]
} 